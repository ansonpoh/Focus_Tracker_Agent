services:
  focus-tracker:
    build:
      context: .
    container_name: focus-tracker-agent
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./config:/app/config
      - ./data:/app/data
      - ./reports:/app/reports
    command: ["python", "main.py"]
